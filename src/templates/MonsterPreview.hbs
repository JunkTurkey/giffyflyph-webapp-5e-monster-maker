<div class="monster theme-{{monster.display.theme}} columns-{{monster.display.columns}} {{#if display.quickstart}}quickstart rank-{{monster.description.rank}}{{/if}}">
	{{#if display.imageBanner}}
		<div class="monster-image banner">
			<img src="{{monster.display.image.url}}"/>
		</div>
	{{/if}}
	<div class="monster-contents">
		<div class="monster-contents-header"></div>
		<div class="monster-contents-body">
			{{#if display.imageInline}}
				<div class="monster-image inline">
					<img src="{{monster.display.image.url}}"/>
				</div>
			{{/if}}
			{{#if display.header}}
				<div class="monster-header">
					<div>
						{{#if display.headerTitle}}
							<h4>{{monster.description.name}}</h4>
						{{/if}}
						{{#if display.headerDescription}}
							<p class="monster-description">{{fmtCapitalise (fmtMonsterDescription monster.description.size monster.description.type monster.tags monster.description.alignment)}}</p>
						{{/if}}
					</div>
					{{#if display.quickstart}}
						<div class="monster-quickstart">
							<p>Level {{monster.description.level}} {{fmtCapitalise monster.description.role}}<br/> {{fmtCapitalise monster.description.rank}} <span class="solo-only">vs {{monster.description.players}} </span>({{fmtLocaleString monster.challenge.xp}} XP)</p>
						</div>
					{{/if}}
				</div>
			{{/if}}
			{{#if display.defences}}
				<hr/>
				<ul>
					{{#if display.armorClass}}
						<li class="monster-ac">
							<p>
								<span class="label">Класс Защиты</span>
								<span>
									{{#if monster.ac.value}}{{monster.ac.value}} {{/if}}
									{{#if monster.ac.type}}({{monster.ac.type}}){{/if}}
								</span>
							</p>
						</li>
					{{/if}}
					{{#if display.hitPoints}}
						<li class="monster-hp">
							<p>
								<span class="label">Хиты</span>
								<span>
									{{#if monster.hp.average}}{{monster.hp.average}} {{/if}}
									{{#if monster.hp.roll}}({{monster.hp.roll}}){{/if}}
								</span>
							</p>
						</li>
					{{/if}}
					{{#if display.speeds}}
						<li class="monster-speed">
							<p>
								<span class="label">Скорость</span>
								<span>{{{fmtMonsterSpeeds monster.speeds}}}</span>
							</p>
						</li>
					{{/if}}
				</ul>
			{{/if}}
			<hr/>
			<div class="monster-abilities">
				<div class="monster-ability">
					<span class="label">Сил</span>
					<span>{{monster.abilities.str.score}} <span class="modifier">({{{fmtModifier monster.abilities.str.modifier}}})</span></span>
				</div>
				<div class="monster-ability">
					<span class="label">Лов</span>
					<span>{{monster.abilities.dex.score}} <span class="modifier">({{{fmtModifier monster.abilities.dex.modifier}}})</span></span>
				</div>
				<div class="monster-ability">
					<span class="label">Тел</span>
					<span>{{monster.abilities.con.score}} <span class="modifier">({{{fmtModifier monster.abilities.con.modifier}}})</span></span>
				</div>
				<div class="monster-ability">
					<span class="label">Инт</span>
					<span>{{monster.abilities.int.score}} <span class="modifier">({{{fmtModifier monster.abilities.int.modifier}}})</span></span>
				</div>
				<div class="monster-ability">
					<span class="label">Муд</span>
					<span>{{monster.abilities.wis.score}} <span class="modifier">({{{fmtModifier monster.abilities.wis.modifier}}})</span></span>
				</div>
				<div class="monster-ability">
					<span class="label">Хар</span>
					<span>{{monster.abilities.cha.score}} <span class="modifier">({{{fmtModifier monster.abilities.cha.modifier}}})</span></span>
				</div>
			</div>
			{{#if display.stats}}
				<hr/>
				<ul class="monster-stats">
					{{#if display.savingThrows}}
						<li class="monster-saves">
							<p>
								<span class="label">{{#if monster.attack.isActive}}Спасброски{{/if}}{{#unless monster.attack.isActive}}Спасброски{{/unless}}</span>
								<span>
									{{#if display.quickstart}}
										{{#if monster.attack.isActive}}
											{{{fmtMonsterSavingThrowsQuickstartActive monster.savingThrows}}}
										{{/if}}
										{{#unless monster.attack.isActive}}
											{{fmtMonsterSavingThrowsQuickstart monster.savingThrows}}
										{{/unless}}
									{{/if}}
									{{#unless display.quickstart}}
										{{fmtMonsterSavingThrows monster.savingThrows}}
									{{/unless}}
								</span>
							</p>
						</li>
					{{/if}}
					{{#if display.skills}}
						<li class="monster-skills">
							<p>
								<span class="label">Навыки</span>
								<span>
									{{#each monster.skills}}
										{{fmtCapitalise name}} {{fmtModifier modifier}}{{#unless @last}}, {{/unless}}
									{{/each}}
								</span>
							</p>
						</li>
					{{/if}}
					{{#if display.vulnerabilities}}
						<li class="monster-vulnerabilities">
							<p>
								<span class="label">Уязвимости к урону</span>
								<span>
									{{#each monster.vulnerabilities}}
										{{this}}{{#unless @last}}, {{/unless}}
									{{/each}}
								</span>
							</p>
						</li>
					{{/if}}
					{{#if display.resistances}}
						<li class="monster-resistances">
							<p>
								<span class="label">Сопротивления урону</span>
								<span>
									{{#each monster.resistances}}
										{{this}}{{#unless @last}}, {{/unless}}
									{{/each}}
								</span>
							</p>
						</li>
					{{/if}}
					{{#if display.immunities}}
						<li class="monster-immunities">
							<p>
								<span class="label">Иммунитет к урону</span>
								<span>
									{{#each monster.immunities}}
										{{this}}{{#unless @last}}, {{/unless}}
									{{/each}}
								</span>
							</p>
						</li>
					{{/if}}
					{{#if display.conditions}}
						<li class="monster-conditions">
							<p>
								<span class="label">Иммунитет к состояниям</span>
								<span>
									{{#each monster.conditions}}
										{{this}}{{#unless @last}}, {{/unless}}
									{{/each}}
								</span>
							</p>
						</li>
					{{/if}}
					{{#if display.senses}}
						<li class="monster-senses">
							<p>
								<span class="label">Чувства</span>
								<span>{{fmtMonsterSenses monster.senses}}</span>
							</p>
						</li>
					{{/if}}
					{{#if display.languages}}
						<li class="monster-languages">
							<p>
								<span class="label">Языки</span>
								<span>
									{{#each monster.languages}}
										{{fmtCapitalise this}}{{#unless @last}}, {{/unless}}
									{{/each}}
								</span>
							</p>
						</li>
					{{/if}}
					{{#if display.challenge}}
						<li class="monster-challenge">
							<p>
								<span class="label">Опасность</span>
								<span>
									{{{monster.challenge.rating}}} {{#unless display.quickstart}}({{fmtLocaleString monster.challenge.xp}} опыта){{/unless}}
								</span>
							</p>
						</li>
					{{/if}}
				</ul>
			{{/if}}
			{{#if display.quickstart}}
				<hr/>
				<ul class="quickstart-helpers">
					<li>
						<p>
							<span class="label">Атаки</span>
							<span>{{#if monster.attack.isActive}}DC {{monster.attack.bonus}} против КЗ{{#if monster.attack.isActiveSmaller}} − 10{{/if}}{{/if}}{{#unless monster.attack.isActive}}{{fmtModifier monster.attack.bonus}} к попаданию{{/unless}}. <i>Hit:</i> {{monster.attack.damage}} damage</span>
						</p>
					</li>
					<li>
						<p>
							<span class="label">КС Атак</span>
							<span>Основной КС {{monster.dc.primary}}, Вторичный КС {{monster.dc.secondary}}</span>
						</p>
					</li>
					{{#if monster.attack.isActive}}
						<li>
							<p>
								<span class="label">Активная Защита</span>
								<span>{{#if monster.attack.isActiveSmaller}}<i>(Smaller Variant) </i>{{/if}}Вы не совершаете бросок атаки - вместо этого цель совершает бросок защиты.</span>
							</p>
						</li>
					{{/if}}
				</ul>
			{{/if}}
			<hr/>
			{{#if display.traits}}
				<h5 class="h5-traits">Особенности</h5>
				<div class="h5-border"></div>
				{{#each monster.traits}}
					<div class="monster-trait">
						<p>{{#if name}}<span class="name">{{name}}.</span>{{/if}}{{#if detail}} <span class="detail">{{{detail}}}</span>{{/if}}</p>
					</div>
				{{/each}}
			{{/if}}
			{{#if display.actions}}
				<h5>Действия</h5>
				<div class="h5-border"></div>
				{{#each monster.actions}}
					<div class="monster-action">
						<p>{{#if name}}<span class="name">{{name}}.</span>{{/if}}{{#if detail}} <span class="detail">{{{detail}}}</span>{{/if}}</p>
					</div>
				{{/each}}
			{{/if}}
			{{#if display.reactions}}
				<h5>Реакции</h5>
				<div class="h5-border"></div>
				{{#each monster.reactions}}
					<div class="monster-reaction">
						<p>{{#if name}}<span class="name">{{name}}.</span>{{/if}}{{#if detail}} <span class="detail">{{{detail}}}</span>{{/if}}</p>
					</div>
				{{/each}}
			{{/if}}
			{{#if display.paragonActions}}
				<h5>Действия парагона</h5>
				<div class="h5-border"></div>
				<p class="paragon-actions">Существо может совершить {{monster.paragonActions}} действия парагона, выбирая между движением или действием. Только одно действие парагона может быть использовано единовременно и только в конце хода другого существа. Существо восстанавливает использованные действия парагона в начале своего хода..</p>
			{{/if}}
			{{#if display.legendaryActions}}
				<h5>Легендарные Действия</h5>
				<div class="h5-border"></div>
				{{#if display.legendaryActionsPerRound}}
					<p class="legendary-per-round">Существо может совершить {{monster.legendaryActionsPerRound}} легендарных действия, выбрав из предложенных ниже вариантов. Одновременно можно использовать только один вариант легендарного действия и только в конце хода другого существа. Существо восстанавливает потраченные легендарные действия в начале своего хода.</p>
				{{/if}}
				{{#each monster.legendaryActions}}
					<div class="monster-legendary-action">
						<p>{{#if name}}<span class="name">{{name}}.</span>{{/if}}{{#if detail}} <span class="detail">{{{detail}}}</span>{{/if}}</p>
					</div>
				{{/each}}
			{{/if}}
			{{#if display.lairActions}}
				<h5>Действия логова</h5>
				<div class="h5-border"></div>
				{{#if display.lairActionsInitiative}}
					<p class="lair-initiative">На счете инициативы {{monster.lairActionsInitiative}} (уступая всем в случае равенство), существо может совершить действие логова, выбрав один из следующих эффектов. При этом нельзя использовать один и тот же эффект дважды подряд.</p>
				{{/if}}
				{{#each monster.lairActions}}
					<div class="monster-lair-action">
						<p>{{#if name}}<span class="name">{{name}}.</span>{{/if}}{{#if detail}} <span class="detail">{{{detail}}}</span>{{/if}}</p>
					</div>
				{{/each}}
			{{/if}}
			{{#if display.notes}}
				<h5 class="h5-notes">Примечания</h5>
				<div class="h5-border"></div>
				<div class="monster-notes">
					{{#each monster.notes}}
						<p class="note">{{{this}}}</p>
					{{/each}}
				</div>
			{{/if}}
			<p class="monster-footer">Сделано с помощью редактора Киборгов и Чародеев</p>
		</div>
		<div class="monster-contents-footer"></div>
	</div>
</div>
<div>
	{{#if display.downloadPng}}
		<button type="button" class="btn btn-sm btn-primary btn-png">
			<span class="fas fa-camera"></span> Сохранить как PNG
		</button>
	{{/if}}
	<button type="button" class="btn btn-sm btn-primary btn-columns icon-only icon-border">
		<span class="fas fa-columns"></span>
	</button>
</div>
